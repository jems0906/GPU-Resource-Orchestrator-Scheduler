import { JobSubmissionForm } from '@/components/Jobs/JobSubmissionForm'

export default function SubmitJobPage() {
  return (
    <div>
      <h2 className="text-white font-bold text-lg mb-6">Submit a New Job</h2>
      <JobSubmissionForm />
    </div>
  )
}
